{
  "name": "example-netlify-edge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node ./build.js",
    "check": "exit 0",
    "start": "pnpm build && netlify dev"
  },
  "dependencies": {
    "graphql": "16.12.0",
    "graphql-yoga": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "0.27.0",
    "netlify-cli": "^23.0.0"
  }
}
