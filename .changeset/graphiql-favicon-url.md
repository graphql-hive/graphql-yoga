---
'graphql-yoga': patch
---

Serve the favicon of the built-in GraphiQL and landing pages from a file that stays in the repository (`packages/render-graphiql/favicon.ico`) now that the website sources moved.
