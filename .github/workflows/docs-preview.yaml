name: docs-preview

# Asks the-guild-org/website (which builds and serves the Yoga docs at
# the-guild.dev/graphql/yoga-server and the Envelop docs at
# the-guild.dev/graphql/envelop) to deploy a preview built with this PR's
# content. Same-repo PRs only: the dispatch needs a secret, which fork PRs
# don't receive — a maintainer can trigger the website repo's
# yoga-docs-preview workflow manually for those.

on:
  pull_request:
    paths:
      - website/**
      - envelop-website/**
      - packages/envelop/**/README.md
      - packages/envelop/core/docs/*.md

jobs:
  request-preview:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: request preview build
        env:
          TOKEN: ${{ secrets.WEBSITE_DISPATCH_TOKEN }}
        run: |
          curl --fail-with-body -X POST \
            -H "Authorization: Bearer $TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/the-guild-org/website/dispatches \
            -d "{\"event_type\":\"yoga-docs-preview\",\"client_payload\":{\"pr\":\"${{ github.event.pull_request.number }}\"}}"

      - name: comment preview link
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          base="https://yoga-pr-${PR}.guild-dev-website.pages.dev"
          marker="<!-- docs-preview-link -->"
          body="${marker}
          📖 Docs preview (rebuilds on every push, ready ~10 min after each):
          - Yoga: ${base}/graphql/yoga-server
          - Envelop: ${base}/graphql/envelop"
          existing=$(gh api "repos/${{ github.repository }}/issues/${PR}/comments" \
            --jq ".[] | select(.body | startswith(\"${marker}\")) | .id" | head -1)
          if [ -z "$existing" ]; then
            gh api "repos/${{ github.repository }}/issues/${PR}/comments" -f body="$body" >/dev/null
          fi
